# Krypton — Frontend

**Pendiente.** Acá va la SPA (React o Angular, a definir) que consume la API del
backend.

Cuando se decida el stack, este README documenta cómo levantarlo. Por ahora:

- El backend vive en [`../backend/`](../backend).
- La guía de arranque del proyecto está en [`../docs/onboarding.md`](../docs/onboarding.md).
