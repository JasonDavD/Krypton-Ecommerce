import { Component } from '@angular/core';

/**
 * Stub component for /catalog.
 * Replace this component with the real implementation in the catalog feature change.
 * Do NOT rename the export — app.routes.ts references this via loadComponent.
 */
@Component({
  selector: 'app-catalog',
  standalone: true,
  template: `
    <section>
      <h2>Catálogo</h2>
      <p>Próximamente — módulo de catálogo en desarrollo.</p>
    </section>
  `,
})
export class CatalogComponent {}
