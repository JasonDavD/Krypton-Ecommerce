import { Component } from '@angular/core';

/**
 * Stub component for /orders.
 * Replace this component with the real implementation in the orders feature change.
 * Do NOT rename the export — app.routes.ts references this via loadComponent.
 */
@Component({
  selector: 'app-orders',
  standalone: true,
  template: `
    <section>
      <h2>Mis pedidos</h2>
      <p>Próximamente — módulo de pedidos en desarrollo.</p>
    </section>
  `,
})
export class OrdersComponent {}
