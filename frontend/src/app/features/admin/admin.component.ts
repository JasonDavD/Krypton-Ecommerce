import { Component } from '@angular/core';

/**
 * Stub component for /admin.
 * Replace this component with the real implementation in the admin feature change.
 * Do NOT rename the export — app.routes.ts references this via loadComponent.
 */
@Component({
  selector: 'app-admin',
  standalone: true,
  template: `
    <section>
      <h2>Administración</h2>
      <p>Próximamente — panel de administración en desarrollo.</p>
    </section>
  `,
})
export class AdminComponent {}
