import { Component } from '@angular/core';

/**
 * Stub component for /reports.
 * Replace this component with the real implementation in the reports feature change.
 * Do NOT rename the export — app.routes.ts references this via loadComponent.
 */
@Component({
  selector: 'app-reports',
  standalone: true,
  template: `
    <section>
      <h2>Reportes</h2>
      <p>Próximamente — módulo de reportes en desarrollo.</p>
    </section>
  `,
})
export class ReportsComponent {}
