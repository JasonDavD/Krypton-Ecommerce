import { Component } from '@angular/core';

/**
 * Stub component for /cart.
 * Replace this component with the real implementation in the cart feature change.
 * Do NOT rename the export — app.routes.ts references this via loadComponent.
 */
@Component({
  selector: 'app-cart',
  standalone: true,
  template: `
    <section>
      <h2>Carrito</h2>
      <p>Próximamente — módulo de carrito en desarrollo.</p>
    </section>
  `,
})
export class CartComponent {}
