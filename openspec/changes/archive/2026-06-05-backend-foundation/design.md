# Design: Backend Foundation

## Technical Approach

Scaffolding Spring Boot 3.3.x (Maven, **target Java 17**; JDK 21 instalado compila a 17 vía
`<java.version>17</java.version>`). El esquema (8 tablas) se materializa con **DDL escrito a
mano en Flyway `V1`**; las 8 entidades JPA + 3 enums se mapean con `ddl-auto: validate`, de modo
que Hibernate **garantiza la paridad** entidad↔schema al arrancar. 8 repositories Spring Data.
Verificado con un test de contexto + un test de integración Testcontainers.

## Architecture Decisions

| Decisión | Elección | Rationale |
|---|---|---|
| Target Java | **17** (JDK 21 instalado) | Alinear con clase/corrección; bytecode solo es compatible hacia adelante (17 corre en JVM 21, no al revés) |
| Dueño del schema | **Flyway** (DDL a mano) + Hibernate `validate` | Schema versionado, sin auto-gen ni drift; paridad dev/prod |
| PK | `BIGINT GENERATED BY DEFAULT AS IDENTITY` + `@GeneratedValue(IDENTITY)` | Estándar, simple, valida limpio |
| Dinero | `NUMERIC(12,2)` + `BigDecimal(precision=12, scale=2)` | Precisión monetaria, sin errores de float |
| Timestamps | `TIMESTAMPTZ` + `Instant` (UTC) | Sin ambigüedad de zona horaria; Hibernate 6 mapea `Instant`→`timestamptz` |
| Enums | `VARCHAR` + `@Enumerated(STRING)` | Legible y a prueba de reordenamientos |
| Base real en tests | **Testcontainers `postgres:16`**, NO H2 | Constraints, kardex y tipos se comportan igual que prod |
| `open-in-view` | `false` | Evita el anti-patrón de lazy-loading en la vista |

## Data Flow — arranque

```
App start
   │
   ▼
Flyway ── aplica migraciones pendientes (V1) ──▶ flyway_schema_history
   │
   ▼
Hibernate (ddl-auto: validate) ── compara 8 entidades ↔ schema
   │   mismatch? ──sí──▶ FAIL FAST (no arranca)
   │   ok
   ▼
Contexto Spring listo ✅
```

## File Changes

| File | Action | Description |
|---|---|---|
| `pom.xml` | Create | Boot 3.3.x, java.version=17, deps + build plugin |
| `pe/com/krypton/KryptonApplication.java` | Create | `@SpringBootApplication` main |
| `pe/com/krypton/model/*.java` (8) | Create | Entidades JPA |
| `pe/com/krypton/model/enums/*.java` (3) | Create | `Role`, `OrderStatus`, `MovementType` |
| `pe/com/krypton/repository/*.java` (8) | Create | Interfaces `JpaRepository` |
| `src/main/resources/application.yml` | Create | Perfiles `default`/`local`, datasource, jpa, flyway |
| `db/migration/V1__create_initial_schema.sql` | Create | DDL de las 8 tablas |
| `src/test/java/.../KryptonApplicationTests.java` | Create | Context load test |
| `src/test/java/.../SchemaIntegrationTest.java` | Create | Testcontainers: V1 aplica, 8 tablas |

## Interfaces / Contracts

DDL `V1` (patrón — tipos no obvios):

```sql
CREATE TABLE products (
    id          BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    sku         VARCHAR(60)  NOT NULL UNIQUE,
    price       NUMERIC(12,2) NOT NULL,
    stock       INT          NOT NULL DEFAULT 0,
    active      BOOLEAN      NOT NULL DEFAULT TRUE,
    category_id BIGINT       NOT NULL REFERENCES categories(id)
);
CREATE TABLE cart (
    id      BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    user_id BIGINT NOT NULL UNIQUE REFERENCES users(id)   -- 1:1
);
```

Mapeo (los bits que valida Hibernate):

```java
@Id @GeneratedValue(strategy = GenerationType.IDENTITY) Long id;
@Column(nullable = false, precision = 12, scale = 2) BigDecimal price;
@Enumerated(EnumType.STRING) @Column(nullable = false) MovementType type;
@ManyToOne(fetch = FetchType.LAZY) @JoinColumn(name = "category_id") Category category;
Instant createdAt;   // ↔ timestamptz
```

## Testing Strategy

| Layer | What | Approach |
|---|---|---|
| Integration | Contexto carga | `@SpringBootTest` + Testcontainers |
| Integration | V1 aplica, 8 tablas, `validate` pasa | `@Testcontainers` `postgres:16` `@ServiceConnection` |
| Unit | — | No hay lógica de negocio en este cambio |

## Migration / Rollout

Flyway `V1` ES la migración. Sin migración de datos (greenfield). Rollback:
`docker compose down -v` (resetea DB) + `git revert`.

## Open Questions

- [ ] JaCoCo (coverage) — diferido, opcional; se agrega si la rúbrica lo exige.
