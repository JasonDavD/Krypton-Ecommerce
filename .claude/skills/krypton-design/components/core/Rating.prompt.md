Read-only star rating in brand yellow (Lucide stars). Call `lucide.createIcons()` after mount.

```jsx
<Rating value={4.5} count={128} showValue />
```

Props: `value` (0–5), `count`, `size`, `showValue`.
