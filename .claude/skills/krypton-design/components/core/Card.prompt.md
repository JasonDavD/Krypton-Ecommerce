White surface container using the brand radius + navy-tinted shadow.

```jsx
<Card>Contenido</Card>
<Card interactive elevation="sm">Sube al hacer hover</Card>
```

Props: `padding`, `elevation` (`none|sm|md|lg`), `interactive` (hover lift), `as`.
